&comppar
    periodic = F       ! Use periodical boundaries in x direction
                       ! Only makes sense for vfuncnr=0 (zero velocity)
                       ! and flowrate=.true. (Poiseuille flow).
    flowrate = F       ! Impose flowrate in x-direction (for
                       ! periodic=.true. only)
    move_mesh_x = F    ! Move the mesh in x-direction to keep the center
                       ! of volume of the drop at the same position wrt
                       ! to the mesh
    monitor_func = F   ! Use a monitor function to move the surface
                       ! elements tangential to high curvature regions.
    mesh_plot = T      ! write mesh plot files/info (curves, mesh etc.)
    compute_error = F  ! Compute error. Only makes sense for eta_e=eta_i
                       ! (no viscosity difference) and gammac=0 (no
                       ! surface tension) or if vfuncnr=0 (no flow)
                       ! or vfuncnr=3 (rotation).
    compute_volume = T ! Compute volume and center of volume. 
    n=5                ! number of elements in x and y direction of box
    nc=80              ! number of elements on the circle
    gauss = 4          ! order of integration for triangular elements
    gaussb = 4         ! number of integration points of line elements
    vfuncnr = 4        ! function number for the velocity field u
    choice_um = 2      ! choice for um in the motion of the interface:
                       !  0: um = 0
                       !  1: um = average velocity of all interface nodes
                       !  2: um = velocity of the center of volume 
    vtkevery = 1       ! vtk file every vtkevery steps. -1: means none
    ctime = T          ! continue time in output after restart
    restart = 0        ! restart=0: no restart
                       ! restart=1: normal restart
                       ! restart=2: restart, but start with Euler step
    restart_every = 0 ! write restart file every restart_every steps.
                       ! -1: means none
    timeint = 2        ! time integration scheme
    timeint_sc = 2     ! time integration scheme for surfactant
                       ! concetration
    numtimesteps = 1000 ! number of time steps
    ox = -5.0          ! x-coordinate left lower corner
    oy = 0.0           ! y-coordinate left lower corner
    lx = 10.0          ! length of the domain
    ly = 5.0           ! height of the domain
    xc = 0.0 0.0       ! center of the initial circle
    rc = 1.0           ! initial radius of the circle
    eta_e = 1.0        ! external fluid viscosity
    eta_i = 1.0        ! internal fluid viscosity
    beta_st = 0.5      ! surface tension beta coefficient
    gammac = 2.0       ! initial surface tension
    c0 = 1.0           ! initial surfactant concentration
    Dcoef = 1.0        ! surfactant concentration diffusion coefficient
    factor = 100.0     ! factor for the interface grid deformation
    U_avg  = 0.25      ! imposed average velocity for flowrate=.true.
    rs_up  = 1.4,      ! real_storage velocity-pressure LU HSL
    is_up  = 1.5,      ! integer_storage velocity-pressure LU HSL
    deltat = 1.e-3     ! time step
    beta = 0.5         ! factor beta in SUPG
    fac_min = 0.5,     ! factor setting dx_min for the monitor function
    fac_max = 2.0,     ! factor setting dx_max for the monitor function
!     area_threshold = 1.39         ! remeshing area threshold
!     aspect_ratio_threshold = 1.39 ! remeshing aspect ratio threshold
   area_threshold = 0.2          ! remeshing area threshold
   aspect_ratio_threshold = 0.2  ! remeshing aspect ratio threshold
/
